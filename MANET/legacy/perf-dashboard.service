# DEPRECATED: perf dashboard is now served by mesh-status (manet_web) on port 80.
# This unit is kept as a no-op so existing nodes don't fail on upgrade.
[Unit]
Description=MANET Performance Dashboard (deprecated — merged into mesh-status)

[Service]
Type=oneshot
ExecStart=/bin/true
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
