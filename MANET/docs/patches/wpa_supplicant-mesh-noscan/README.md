Moved — the two OpenWrt patches archived here (and their own README) now
live at `MANET/src/wpa-supplicant-mesh/` alongside the binary they build
and the `build.sh` recipe that uses them (they're build inputs, not
documentation). See that directory's `README.md`.
